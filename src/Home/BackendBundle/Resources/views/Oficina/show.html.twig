{% extends 'MWSimpleCrudGeneratorBundle::layout.html.twig' %}

{% block title %}
{{ parent() }} - {{ 'views.show.show'|trans({'%entity%': 'Oficina'}, 'MWSimpleCrudGeneratorBundle') }}
{% endblock %}

{% block page %}
    
<div class="well well-lg">
    <h1>Oficina</h1>
    <form class="form-horizontal">
        <fieldset>                <div class="form-group">
                    <label class="control-label">Id</label>
                    <div class="form-control">                        <span class="input-xlarge uneditable-input">{{ entity.id }}</span>                    </div>
                </div>                <div class="form-group">
                    <label class="control-label">Nombre</label>
                    <div class="form-control">                        <span class="input-xlarge uneditable-input">{{ entity.nombre }}</span>                    </div>
                </div>                <div class="form-group">
                    <label class="control-label">Denominacioncorta</label>
                    <div class="form-control">                        <span class="input-xlarge uneditable-input">{{ entity.denominacionCorta }}</span>                    </div>
                </div>                <div class="form-group">
                    <label class="control-label">Oficue</label>
                    <div class="form-control">                        <span class="input-xlarge uneditable-input">{{ entity.ofiCUE }}</span>                    </div>
                </div>                <div class="form-group">
                    <label class="control-label">Oficui</label>
                    <div class="form-control">                        <span class="input-xlarge uneditable-input">{{ entity.ofiCUI }}</span>                    </div>
                </div>                <div class="form-group">
                    <label class="control-label">Jurmarcbaj</label>
                    <div class="form-control">                        <span class="input-xlarge uneditable-input">{{ entity.jurMarcBaj }}</span>                    </div>
                </div>        </fieldset>
    </form>
</div>

        <div class="col-lg-9">
    <div class="float-left col-lg-3">
        <a class="btn btn-info col-lg-11" href="{{ path('admin_oficina') }}">
            {{ 'views.recordactions.backtothelist'|trans({}, 'MWSimpleCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left col-lg-3">
        <a class="btn btn-primary col-lg-11" href="{{ path('admin_oficina_edit', { 'id': entity.id }) }}">
            {{ 'views.recordactions.edit'|trans({}, 'MWSimpleCrudGeneratorBundle') }}
        </a>
    </div>
    <div class="float-left col-lg-3">{{ form(delete_form) }}</div>
</div>
{% endblock %}
